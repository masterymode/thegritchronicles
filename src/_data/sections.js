export default {
  // "Get the book" card — turn on once /book exists and the book is live.
  showProductCard: true,

  // Newsletter eyebrow + copy + subscribe form.
  showNewsletter: true,

  // Fixed background video (public/background.mp4). Stays off until the
  // file is actually in /public, otherwise it's a broken video tag.
  showBackgroundVideo: true,

  // Social icon row under the logo (placeholder links for now).
  showSocialRow: true,
};
